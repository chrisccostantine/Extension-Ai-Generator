{
  "name": "shopify-ai-generator-app",
  "private": true,
  "version": "0.1.0",
  "description": "Starter scaffold for migrating the AI product generator into a real Shopify app",
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "generate:extension": "shopify app generate extension"
  }
}
